{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ product.name }}</h1>
    <h2>{{ product.categorie.name }}</h2>
    <p>Mis en ligne le {{ product.createAt | date("m.d.y") }}</p>
    <img  src="/img/uploads/{{ product.imageNAme }}"  />
    <h2>{{ product.price }} €</h2>
    <h3>{{ product.description }}</h3>
    <h3>{{ product.nbViews }}</h3>

    <a href="{{ path('app_produit_liste') }}" class="card-link btn btn-outline-info">Retour liste</a>
{% endblock %}

{% block title %}

{% endblock %}

